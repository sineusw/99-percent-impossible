const $=s=>document.querySelector(s),K=n=>'n99_'+n,G=(n,d=0)=>{let v=localStorage.getItem(K(n));return v===null?d:+v},S=(n,v)=>localStorage.setItem(K(n),v);let st={g:null,run:0,ready:0,start:0,raf:0,to:0,pos:1,dir:1,tgt:null,ctx:null,tick:0,share:'',locked:0};const names={timer:['PERFECT TIMER','Stop at exactly 1.000 seconds. The timer hides after 0.500s — keep counting in your head.'],stop:['PERFECT STOP','Tap START, then stop the moving white marker inside the bright green target.'],reaction:['REACTION TEST','Tap START, wait for the entire box to turn bright green, then tap the green box.']};const home=$('#home'),game=$('#game'),title=$('#title'),prompt=$('#prompt'),play=$('#play'),res=$('#res'),primary=$('#primary'),best=$('#best'),att=$('#att'),tot=$('#tot'),bst=$('#bst'),modal=$('#modal'),mg=$('#mg'),ms=$('#ms'),mm=$('#mm'),mr=$('#mr'),back=$('#back'),closeBtn=$('#close'),retryBtn=$('#retry'),copyBtn=$('#copy');
function stats(){tot.textContent=G('total');bst.textContent=G('bestStreak');if(st.g)att.textContent=G(st.g+'_attempts')}function bump(){S('total',G('total')+1);S(st.g+'_attempts',G(st.g+'_attempts')+1);stats()}function streak(ok){let c=ok?G('currentStreak')+1:0;S('currentStreak',c);if(c>G('bestStreak'))S('bestStreak',c);stats()}function pb(){let v=localStorage.getItem(K(st.g+'_best'));if(v===null)return'—';v=+v;return st.g==='reaction'?Math.round(v)+'ms':st.g==='timer'?v.toFixed(3)+'s':v.toFixed(1)+'%'}
function audio(){let A=window.AudioContext||window.webkitAudioContext;if(!A)return null;if(!st.ctx)st.ctx=new A;if(st.ctx.state==='suspended')st.ctx.resume();return st.ctx}function tone(f,d=.06,t='sine',v=.04,w=0){let c=audio();if(!c)return;let o=c.createOscillator(),g=c.createGain(),s=c.currentTime+w;o.type=t;o.frequency.value=f;g.gain.setValueAtTime(.0001,s);g.gain.exponentialRampToValueAtTime(v,s+.006);g.gain.exponentialRampToValueAtTime(.0001,s+d);o.connect(g).connect(c.destination);o.start(s);o.stop(s+d+.02)}function ticks(mode){untick();let b=mode==='timer'?250:mode==='stop'?190:150,i=0,ms=mode==='reaction'?360:190;st.tick=setInterval(()=>tone(b+Math.min(420,++i*11),.035,mode==='reaction'?'sine':'square',.018),ms)}function untick(){clearInterval(st.tick);st.tick=0}function failSound(){untick();tone(150,.12,'sawtooth',.08);tone(85,.18,'square',.05,.05)}function win(t){untick();let a=t==='perfect'?[392,523,659,784,1047,1319]:t==='epic'?[523,659,784,1047]:t==='strong'?[440,660,880]:[520,660];a.forEach((f,i)=>tone(f,.14,'triangle',t==='perfect'?.09:.06,i*.06));if(t==='perfect')tone(2093,.35,'sine',.08,.38)}
function tier(a){return a>=99.95?'perfect':a>=99.5?'epic':a>=97?'strong':a>=90?'subtle':'fail'}function rtier(ms){return ms<=170?'perfect':ms<=200?'epic':ms<=230?'strong':ms<=280?'subtle':'fail'}function fx(t,isPB,fail){play.className='play';document.body.className='';if(fail){play.classList.add('shake');document.body.classList.add('red');failSound();navigator.vibrate?.([25,35,45]);return}play.classList.add(t);document.body.classList.add(t==='perfect'?'gold':isPB||t==='epic'?'greenbg':'');win(t);navigator.vibrate?.(t==='perfect'?[30,40,70,40,110]:30);if(t==='epic'||t==='perfect')conf(t==='perfect'?65:30)}function conf(n){for(let i=0;i<n;i++){let e=document.createElement('i');e.className='conf';e.style.left=Math.random()*100+'%';e.style.setProperty('--x',(Math.random()-.5)*180+'px');e.style.setProperty('--d',(.8+Math.random()*.8)+'s');e.style.setProperty('--h',(45+Math.random()*70));document.body.appendChild(e);setTimeout(()=>e.remove(),1700)}}
function openGame(g){st.g=g;home.classList.remove('on');game.classList.add('on');title.textContent=names[g][0];prompt.textContent=names[g][1];reset()}function reset(){cancelAnimationFrame(st.raf);clearTimeout(st.to);untick();st.run=st.ready=0;st.locked=0;st.tgt=null;modal.classList.add('hide');res.classList.add('hide');primary.textContent='START';play.className='play';document.body.className='';if(st.g==='timer')play.innerHTML='<div class="timer"><div class="num">0.000</div><div class="note">VISIBLE UNTIL 0.500s</div></div>';if(st.g==='stop')play.innerHTML='<div class="stopstage"><b class="sl">PRESS START TO BEGIN</b><div class="track"><div class="target"></div><div class="marker"></div></div><div class="note">WHITE LINE → GREEN TARGET</div></div>';if(st.g==='reaction')play.innerHTML='<div class="rx"><b>READY?</b><div>Press START below.</div></div>';best.textContent=pb();stats()}
function show(score,roast,meta,share,t,isPB,fail){res.innerHTML='<b>'+score+'</b>'+(isPB?'<div class="new">NEW PERSONAL BEST</div>':'')+'<div>'+roast+'</div>';res.classList.remove('hide');mg.textContent=names[st.g][0];ms.textContent=score;mm.textContent=meta||'';mm.classList.toggle('hide',!meta);mr.textContent=roast;st.share=share;copyBtn.textContent='COPY TIKTOK CAPTION';modal.classList.remove('hide');fx(t,isPB,fail);best.textContent=pb()}
function nearReveal(kind,label,sub,cb){st.locked=1;play.classList.add('nearhold');let v=document.createElement('div');v.className='nearveil '+kind;v.innerHTML='<b>'+label+'</b><span>'+sub+'</span>';play.appendChild(v);let c=audio();if(c){let o=c.createOscillator(),g=c.createGain(),n=c.currentTime;o.type='sine';o.frequency.setValueAtTime(135,n);o.frequency.exponentialRampToValueAtTime(230,n+.36);g.gain.setValueAtTime(.0001,n);g.gain.exponentialRampToValueAtTime(.055,n+.03);g.gain.exponentialRampToValueAtTime(.0001,n+.39);o.connect(g).connect(c.destination);o.start(n);o.stop(n+.4)}setTimeout(()=>{v.remove();play.classList.remove('nearhold');st.locked=0;cb()},380)}
function timerStart(){audio();st.run=1;st.start=performance.now();primary.textContent='STOP';ticks('timer');let n=play.querySelector('.num'),q=play.querySelector('.note');let loop=()=>{let t=(performance.now()-st.start)/1000;if(t<.5){n.textContent=t.toFixed(3);q.textContent='VISIBLE UNTIL 0.500s'}else{n.textContent='???';n.classList.add('green');q.textContent='TIMER HIDDEN — KEEP COUNTING'}st.raf=requestAnimationFrame(loop)};st.raf=requestAnimationFrame(loop)}function timerStop(){cancelAnimationFrame(st.raf);untick();st.run=0;let t=(performance.now()-st.start)/1000,d=Math.abs(1-t),a=Math.max(0,100-d*100),raw=localStorage.getItem(K('timer_best')),old=raw===null?null:+raw,isPB=old===null||d<Math.abs(1-old);bump();if(isPB)S('timer_best',t);streak(a>=97);let ro=d<.0005?'PERFECT. That should not have happened.':d<=.003?'0.00'+Math.max(1,Math.round(d*1000))+'s off. Brutal.':d<=.01?'You can taste 1.000.':d<=.05?'Close. Your thumb has potential.':'A sundial could do better.',ti=tier(a),done=()=>{show(t.toFixed(3)+'s',ro,a.toFixed(1)+'% accuracy',`I hit ${t.toFixed(3)}s (${a.toFixed(1)}%) on Perfect Timer in 99% IMPOSSIBLE 💀 bet you can't get closer to 1.000s.`,ti,isPB,ti==='fail');primary.textContent='TRY AGAIN'};if(a>=98.5&&a<99.5){nearReveal('timer',d.toFixed(3)+'s AWAY','SO CLOSE',done)}else done()}
function stopStart(){audio();st.run=1;primary.textContent='STOP';ticks('stop');let tg=play.querySelector('.target'),m=play.querySelector('.marker'),sl=play.querySelector('.sl'),ss=Math.min(8,G('stop_streak')),w=Math.max(9,22-ss*1.6),x=12+Math.random()*(76-w);tg.style.left=x+'%';tg.style.width=w+'%';sl.textContent='TAP STOP WHEN WHITE HITS GREEN';st.pos=1;st.dir=1;let last=performance.now(),speed=62+ss*8;let loop=now=>{let dt=Math.min((now-last)/1000,.05);last=now;st.pos+=st.dir*speed*dt;if(st.pos>=98)st.pos=98,st.dir=-1;if(st.pos<=1)st.pos=1,st.dir=1;m.style.left=st.pos+'%';st.raf=requestAnimationFrame(loop)};st.tgt={x,w};st.raf=requestAnimationFrame(loop)}function stopStop(){if(!st.tgt)return;cancelAnimationFrame(st.raf);untick();st.run=0;let c=st.tgt.x+st.tgt.w/2,d=Math.abs(st.pos-c),sc=Math.max(0,100-d/50*100),raw=localStorage.getItem(K('stop_best')),old=raw===null?null:+raw,isPB=old===null||sc>old,hit=st.pos>=st.tgt.x&&st.pos<=st.tgt.x+st.tgt.w,edgeDist=hit?0:Math.min(Math.abs(st.pos-st.tgt.x),Math.abs(st.pos-(st.tgt.x+st.tgt.w)));bump();if(isPB)S('stop_best',sc);S('stop_streak',hit?G('stop_streak')+1:0);streak(sc>=97);let ro=hit?(sc>98?'DISGUSTINGLY GOOD.':'Inside. Now do it cleaner.'):(edgeDist<=3?'Your thumb flinched.':'Missed it. Your thumb panicked.'),ti=tier(sc),done=()=>{show(sc.toFixed(1)+'%',ro,hit?'TARGET HIT':'TARGET MISSED',`I got ${sc.toFixed(1)}% on Perfect Stop in 99% IMPOSSIBLE and my phone almost blew up 💀 bet you can't beat this.`,ti,isPB,!hit||ti==='fail');primary.textContent='TRY AGAIN'};if(!hit&&edgeDist<=3){nearReveal('stop',d.toFixed(1)+'% FROM CENTER','MISSED BY '+edgeDist.toFixed(1)+'% OF TRACK',done)}else done()}
function rxStart(){audio();st.run=1;st.ready=0;primary.textContent='WAIT FOR GREEN…';ticks('reaction');let r=play.querySelector('.rx');r.className='rx wait';r.innerHTML='<b>WAIT…</b><div>Do not tap yet. The whole box will turn GREEN.</div>';st.to=setTimeout(()=>{if(!st.run)return;untick();tone(920,.08,'square',.05);st.ready=1;st.start=performance.now();r.className='rx go';r.innerHTML='<b>GO!</b><div>TAP THIS GREEN BOX NOW</div>';primary.textContent='TAP GREEN BOX!';navigator.vibrate?.(30)},1600+Math.random()*1800)}function rxHit(){if(!st.run)return;if(!st.ready){clearTimeout(st.to);untick();st.run=0;bump();streak(false);let r=play.querySelector('.rx');r.className='rx early';r.innerHTML='<b>TOO EARLY</b><div>Wait for BRIGHT GREEN.</div>';show('TOO EARLY','Impatience is not a reflex.','NO TIME RECORDED','I got baited by Reaction Test in 99% IMPOSSIBLE and tapped TOO EARLY 💀 bet you do the same.','fail',false,true);primary.textContent='TRY AGAIN';return}let msr=performance.now()-st.start;st.run=st.ready=0;bump();let raw=localStorage.getItem(K('reaction_best')),old=raw===null?null:+raw,isPB=old===null||msr<old;if(isPB)S('reaction_best',msr);streak(msr<=230);let ro=msr<180?'Okay, are you secretly a cat?':msr<230?'Fast. Annoyingly fast.':msr<=250?'You nearly had your best.':'A refrigerator reacts faster.',ti=rtier(msr),gap=old===null?null:msr-old,done=()=>{show(Math.round(msr)+'ms',ro,msr<=170?'ELITE REACTION':'REACTION TIME',`I hit ${Math.round(msr)}ms on Reaction Test in 99% IMPOSSIBLE ⚡ bet your reflexes are slower.`,ti,isPB,ti==='fail');primary.textContent='TRY AGAIN'};if(msr>=231&&msr<=250&&old!==null&&!isPB&&gap>0){nearReveal('reaction',Math.round(gap)+'ms FROM YOUR BEST','SO CLOSE',done)}else done()}
primary.onpointerup=e=>{e.preventDefault();if(st.locked)return;audio();if(primary.textContent==='TRY AGAIN')return reset();if(st.g==='timer')st.run?timerStop():timerStart();else if(st.g==='stop')st.run?stopStop():stopStart();else if(st.g==='reaction'&&!st.run)rxStart()};play.onpointerup=e=>{if(st.locked)return;if(st.g==='reaction'&&st.run){e.preventDefault();rxHit()}};document.querySelectorAll('[data-g]').forEach(b=>b.onclick=()=>openGame(b.dataset.g));back.onclick=()=>{reset();game.classList.remove('on');home.classList.add('on');st.g=null;stats()};closeBtn.onclick=()=>modal.classList.add('hide');retryBtn.onclick=reset;modal.onclick=e=>{if(e.target===modal)modal.classList.add('hide')};copyBtn.onclick=async()=>{let ok=0;try{await navigator.clipboard.writeText(st.share);ok=1}catch(e){let t=document.createElement('textarea');t.value=st.share;document.body.appendChild(t);t.select();try{ok=document.execCommand('copy')}catch{}t.remove()}copyBtn.textContent=ok?'COPIED ✓':'COPY FAILED — PRESS & HOLD';if(ok){tone(740,.07);tone(980,.08,'sine',.04,.06);navigator.vibrate?.(20)}};stats();