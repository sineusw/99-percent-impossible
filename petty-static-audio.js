/* 99% IMPOSSIBLE — Petty static-first audio transport v1.3
   Normal path: local static MP3 -> runtime ElevenLabs audio.
   First static Audio.play() is invoked synchronously from speechSynthesis.speak(), preserving Safari user activation. */
(()=>{
const synth=window.speechSynthesis;if(!synth||synth.__pettyStaticAudioPatched)return;
const fallbackSpeak=synth.speak.bind(synth),fallbackCancel=synth.cancel.bind(synth),ALLOW_DEVICE_FALLBACK=new URLSearchParams(location.search).get('pettydevice')==='1';let activeAudio=null,activeUrl=null,activeUtterance=null,activeController=null,seq=0;const staticCache=new Map(),runtimeCache=new Map(),existenceCache=new Map();
function hashText(text){let h=0x811c9dc5;for(let i=0;i<text.length;i++){h^=text.charCodeAt(i);h=Math.imul(h,0x01000193)}return(h>>>0).toString(16).padStart(8,'0')}
function pathFor(text){return'/assets/petty-audio/'+hashText(String(text||'').trim())+'.mp3'}
function cleanup(){if(activeController){try{activeController.abort()}catch{}activeController=null}if(activeAudio){try{activeAudio.pause()}catch{}activeAudio.onended=activeAudio.onerror=activeAudio.onplay=null;activeAudio=null}if(activeUrl){try{URL.revokeObjectURL(activeUrl)}catch{}activeUrl=null}}
async function fetchStatic(text,signal){const filePath=pathFor(text);if(existenceCache.get(filePath)===false)throw new Error('static-miss');if(staticCache.has(filePath))return staticCache.get(filePath);const p=fetch(filePath,{cache:'force-cache',signal}).then(async r=>{if(!r.ok){existenceCache.set(filePath,false);throw new Error('static '+r.status)}existenceCache.set(filePath,true);return r.blob()});staticCache.set(filePath,p);try{return await p}catch(e){staticCache.delete(filePath);throw e}}
async function fetchRuntimeFallback(text,signal){if(runtimeCache.has(text))return runtimeCache.get(text);const p=fetch('/api/petty-voice',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({text}),signal}).then(async r=>{if(!r.ok)throw new Error('voice '+r.status);return r.blob()});runtimeCache.set(text,p);try{return await p}catch(e){runtimeCache.delete(text);throw e}}
window.preloadPettyVoice=text=>{const t=String(text||'').trim();if(!t)return Promise.resolve(false);return fetchStatic(t).then(()=>true).catch(()=>false)};window.PettyStaticAudio={hashText,pathFor,allowDeviceFallback:ALLOW_DEVICE_FALLBACK};
function finishUnavailable(utterance,err){try{utterance.onerror?.({type:'error',engine:'none',reason:'voice-unavailable',error:err})}catch{}try{utterance.onend?.({type:'end',engine:'none',unavailable:true})}catch{}}
function finishUtterance(utterance,engine){if(activeUtterance===utterance)activeUtterance=null;try{utterance.onend?.({type:'end',engine})}catch{}cleanup()}
async function playRuntimeFallback(utterance,text,mySeq,staticErr){if(mySeq!==seq)return;const controller=new AbortController();activeController=controller;try{const blob=await fetchRuntimeFallback(text,controller.signal);if(mySeq!==seq)return;if(activeController===controller)activeController=null;activeUrl=URL.createObjectURL(blob);const a=new Audio(activeUrl);activeAudio=a;a.volume=typeof utterance.volume==='number'?utterance.volume:1;a.preload='auto';a.onplay=()=>{try{utterance.onstart?.({type:'start',engine:'runtime'})}catch{}};a.onended=()=>{if(mySeq===seq)finishUtterance(utterance,'runtime')};a.onerror=err=>{if(mySeq!==seq)return;cleanup();if(activeUtterance===utterance)activeUtterance=null;if(ALLOW_DEVICE_FALLBACK){try{fallbackSpeak(utterance)}catch{finishUnavailable(utterance,err)}}else finishUnavailable(utterance,err)};const p=a.play();p?.catch?.(a.onerror)}catch(err){if(mySeq!==seq||err?.name==='AbortError')return;cleanup();if(activeUtterance===utterance)activeUtterance=null;if(ALLOW_DEVICE_FALLBACK){try{fallbackSpeak(utterance)}catch{finishUnavailable(utterance,err)}}else finishUnavailable(utterance,err||staticErr)}}
function speakAudio(utterance,mySeq){const text=String(utterance?.text||'').trim();activeUtterance=utterance;if(!text){utterance.onend?.({type:'end'});activeUtterance=null;return}cleanup();activeUtterance=utterance;const filePath=pathFor(text),a=new Audio(filePath);activeAudio=a;a.volume=typeof utterance.volume==='number'?utterance.volume:1;a.preload='auto';let failed=false;const fallback=err=>{if(failed||mySeq!==seq)return;failed=true;if(activeAudio===a){try{a.pause()}catch{}activeAudio=null}a.onended=a.onerror=a.onplay=null;existenceCache.set(filePath,false);playRuntimeFallback(utterance,text,mySeq,err)};a.onplay=()=>{existenceCache.set(filePath,true);try{utterance.onstart?.({type:'start',engine:'static'})}catch{}};a.onended=()=>{if(mySeq===seq)finishUtterance(utterance,'static')};a.onerror=fallback;try{const p=a.play();p?.catch?.(fallback)}catch(err){fallback(err)}}
synth.speak=function(utterance){const mySeq=++seq;speakAudio(utterance,mySeq)};synth.cancel=function(){seq++;const u=activeUtterance;activeUtterance=null;cleanup();try{fallbackCancel()}catch{}try{u?.onend?.({type:'end',cancelled:true})}catch{}};synth.__pettyStaticAudioPatched=true;window.PETTY_VOICE_ENGINE='static-first';
})();
